<br>
<br>
<br>
<br>
<div style="background-color: #FFFFFF;">
  <div class="container">
    <div class="row">
      <div class="content_form_user">
        <div class="row">

          <div class="col-xs-12 col-sm-offset-4 col-sm-4">
          <h1 style="color:black;"> <strong>Connexion </strong></h1>
          <br><br>

            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

            
                <%= f.label :email, class: "sr-only" %><p><strong>Email</strong></p>
                <%= f.email_field :email, autofocus: true, :class => "email_signin form-control", placeholder: "Tapez votre email..." %>

            <br>
                <%= f.label :password, class: "sr-only" %><p> <strong>Mot de passe</strong></p>
                
                <%= f.password_field :password, autocomplete: "off", :class => "form-control", placeholder: "Tapez votre mot de passe..." %>
                <p><%= link_to "Mot de passe oublié?", new_password_path(resource_name), style: "color: #e0115f;" %></p>
           


            <!--   <% if devise_mapping.rememberable? -%>
                <div class="field">
                  <%= f.check_box :remember_me %>
                  <%= f.label :remember_me, 'Garder ma session ouverte' %>
                </div>
              <% end %> -->

              <br>
              <br>
              <div class="actions text-right" style="margin-top: 10px;">

                <%= f.submit "Connexion", class: "btn btn-sign btn_sign_mobile" %> </p>
                <%= link_to "Créer un compte", new_registration_path(resource_name), class: "btn btn-signup btn_sign_mobile" %><br />

              </div>

          <br>
            <% end %>
            </div>
        </div>
      </div>
    </div>
  </div>
</div>